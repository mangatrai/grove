node_modules
dist
.env
.DS_Store
coverage
# Runtime data: SQLite (+ WAL/SHM), staged uploads under imports/.
# Canonical location is repo root: ./data/ (see backend/src/paths.ts → repoRoot).
# ./backend/data/ can appear if something ran with cwd=backend or an old path bug — treat like ./data/; do not commit.
/data/
.runtime/
oci/oci-retry.sh
docs/payslip-redesign/
docs/real-estate/
docs/pt1-protest/
docs/year-in-review
