services:
  postgres:
    image: postgres:16
    ports:
      - "5433:5432"
    environment:
      POSTGRES_USER: household
      POSTGRES_PASSWORD: household
      POSTGRES_DB: household_finance_test
    volumes:
      - hf_pg_data:/var/lib/postgresql/data

volumes:
  hf_pg_data:
