/** `import_file.payslip_async_provider` when queued for generic OpenAI vision + JSON-schema extract. */
export const OPENAI_LLM_PAYSLIP_PROVIDER = "openai_llm_payslip" as const;

export type PayslipAsyncProviderId = typeof OPENAI_LLM_PAYSLIP_PROVIDER;
